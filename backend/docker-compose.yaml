version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      # Example environment variables. Adjust these based on your backend/app/core/settings.py
      # For a real project, consider using a .env file or Docker secrets for sensitive information.
      DATABASE_URL: "postgresql://user:password@db:5432/mydatabase"
      SECRET_KEY: "your-super-secret-key-for-fastapi"
      # Add other settings required by your application, e.g., for OAuth providers:
      # GOOGLE_CLIENT_ID: "your_google_client_id"
      # GOOGLE_CLIENT_SECRET: "your_google_client_secret"
      # GITHUB_CLIENT_ID: "your_github_client_id"
      # GITHUB_CLIENT_SECRET: "your_github_client_secret"
    # Uncomment and configure if you have a database service (e.g., PostgreSQL, MySQL)
    # depends_on:
    #   - db
    # networks:
    #   - app_network

# Uncomment and configure if you have a database service
# services:
#   db:
#     image: postgres:13-alpine
#     environment:
#       POSTGRES_DB: mydatabase
#       POSTGRES_USER: user
#       POSTGRES_PASSWORD: password
#     volumes:
#       - db_data:/var/lib/postgresql/data
#     networks:
#       - app_network

# Uncomment if you define custom networks or volumes
# volumes:
#   db_data:

# networks:
#   app_network:
#     driver: bridge